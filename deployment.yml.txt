apiVersion: apps/v1
kind: Deployment
metadata:
  name: staticwebsite
  namespace: static
spec:
  selector:
    matchLabels:
      app: staticwebsite
  replicas: 1 # tells deployment to run 2 pods matching the template
  template:
    metadata:
      labels:
        app:staticwebsite
    spec:
      containers:
      - name: staticwebsite
        image:060920346106.dkr.ecr.us-east-2.amazonaws.com/premk8s:22
        ports:
        - containerPort: 80